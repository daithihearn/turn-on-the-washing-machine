es:
    daily_price_subject: Tomorrow's Prices
    daily_rating_good: |
        ```%{date_time} es un dia ```*BUENO*
    daily_rating_bad: |
        ```%{date_time} es un dia ```*MALO*
    daily_rating_normal: |
        ```%{date_time} es un dia ```*NORMAL*
    daily_price_one: |

        ```Un periodo mas barato es de ```*%{cheapest_period_one_start}*``` a ```*%{cheapest_period_one_end}*``` (```*%{cheapest_period_one_price}*``` centimos/kWh).```

        ```El periodo mas caro es de ```*%{expensive_period_start}*``` a ```*%{expensive_period_end}*``` (```*%{expensive_period_price}*``` centimos/kWh)```

        https://elec.daithiapp.com/

    daily_price_two: |

        ```Hay dos periodos baratos. El primer periodo es de ```*%{cheapest_period_one_start}*``` a ```*%{cheapest_period_one_end}*``` (```*%{cheapest_period_one_price}*``` centimos/kWh) y el segundo es de ```*%{cheapest_period_two_start}*``` a ```*%{cheapest_period_two_end}*``` (```*%{cheapest_period_two_price}*``` centimos/kWh).```

        ```El periodo mas caro es de ```*%{expensive_period_start}*``` a ```*%{expensive_period_end}*``` (```*%{expensive_period_price}*``` centimos/kWh)```

        https://elec.daithiapp.com/

    expensive_price_subject: Expensive Price Alert!
    expensive_price: |
        ```Apaga todo!!

        Es el periodo de ```*%{period_length}*``` horas mas caro con un precio medio de ```*%{average_price}*``` centimos/kWh```

        https://elec.daithiapp.com/

    cheap_price_subject: Cheap Price Alert!
    cheap_price: |
        ```Enciende la lavadora!!

        Un periodo de ```*%{period_length}*``` horas barato esta empezando ahora con un precio medio de ```*%{average_price}*``` centimos/kWh```

        https://elec.daithiapp.com/
