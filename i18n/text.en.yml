en:
    daily_price_subject: Tomorrow's Prices
    daily_rating_good: |
        ```%{date_time} is a ```*GOOD*``` day```
    daily_rating_bad: |
        ```%{date_time} is a ```*BAD*``` day```
    daily_rating_normal: |
        ```%{date_time} is a ```*NORMAL*``` day```

    daily_price_one: |

        ```The cheapest period is ```*%{cheapest_period_one_start}*``` to ```*%{cheapest_period_one_end}*``` (```*%{cheapest_period_one_price}*``` cents/kWh).```

        ```The most expensive period is ```*%{expensive_period_start}*``` to ```*%{expensive_period_end}*``` (```*%{expensive_period_price}*``` cents/kWh)```

        https://elec.daithiapp.com/

    daily_price_two: |

        ```There are two cheap periods. One from ```*%{cheapest_period_one_start}*``` to ```*%{cheapest_period_one_end}*``` (```*%{cheapest_period_one_price}*``` cents/kWh) and another from ```*%{cheapest_period_two_start}*``` to ```*%{cheapest_period_two_end}*``` (```*%{cheapest_period_two_price}*``` cents/kWh).```

        ```The most expensive period is ```*%{expensive_period_start}*``` to ```*%{expensive_period_end}*``` (```*%{expensive_period_price}*``` cents/kWh)```

        https://elec.daithiapp.com/
        
    expensive_price_subject: Expensive Price Alert!
    expensive_price: |
        ```Turn everything off!!

        It's the most expensive ```*%{period_length}*``` hour period with an average price of ```*%{average_price}*``` cents/kWh```

        https://elec.daithiapp.com/

    cheap_price_subject: Cheap Price Alert!
    cheap_price: |
        ```Turn on the washing machine!!

        A cheap ```*%{period_length}*``` hour period is starting now with an average price of ```*%{average_price}*``` cents/kWh```

        https://elec.daithiapp.com/
